# # docker-compose.yml

# version: '3.8'

# services:
#   web:
#     build: .
#     command: python manage.py runserver 0.0.0.0:8000
#     volumes:
#       - .:/app:Z  # SELinux compatibility with :Z suffix
#     ports:
#       - "8000:8000"
#     environment:
#       - DEBUG=True
#       - DJANGO_SETTINGS_MODULE=config.settings.development
#       - SECRET_KEY=dev-secret-key-change-in-production
#       - TIMESERIES_API_URL=http://host.docker.internal:8001  # Connect to API on host machine
#     depends_on:
#       - timeseries-api-mock

#   timeseries-api-mock:
#     image: python:3.13-slim
#     volumes:
#       - ./api_mock:/app:Z  # SELinux compatibility with :Z suffix
#     working_dir: /app
#     command: >
#       sh -c "pip install fastapi uvicorn && 
#              uvicorn api_mock:app --host 0.0.0.0 --port 8001"
#     ports:
#       - "8001:8001"
